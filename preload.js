// Preload script for Electron security
// This runs in the renderer context with Node.js access
// but exposes only what we want to the main window

// Currently empty — game doesn't need preload bridge
// Add IPC handlers here if needed in future
